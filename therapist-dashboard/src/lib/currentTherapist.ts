// No auth in this MVP (see docs/CLAUDE.md). The dashboard is used as Proud A.
// (PT001), who is already the assigned PT for every patient in the mock data.
export const CURRENT_THERAPIST_ID = "PT001";
